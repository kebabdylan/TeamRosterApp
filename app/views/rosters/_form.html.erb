<%= form_for(@roster) do |f| %>
  <% if @roster.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@roster.errors.count, "error") %> prohibited this roster from being saved:</h2>

      <ul>
      <% @roster.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :team %><br />
    <%= f.collection_select :team_id, Team.all, :id, :name, :prompt=>true %>
  </div>
  <div class="field">
    <%= f.label :member %><br />
    <%= f.collection_select :member_id, Member.all, :id, :full_name, :prompt=>true %>
  </div>
  <div class="field">
    <%= f.label :role %><br />
    <%= f.collection_select :role_id, Role.all, :id, :name, :prompt=>true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
