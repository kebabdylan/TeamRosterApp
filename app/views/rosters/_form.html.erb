<%= form_for @roster, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :team_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :team_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :member_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :member_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :role_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :role_id, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                rosters_path, :class => 'btn' %>
  </div>
<% end %>
