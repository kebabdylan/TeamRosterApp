<h2>Your Projects</h2>
<p>You have <%= @projects.length %> total project(s)</p>
<% @projects.each do |project| %>
	
	<div class="item-box">
		<h3><%= project.title %></h3>
		<p><%= project.description %>
		<p><%= link_to "view", "/me/project/#{project.id}"	%></p>
		
		
	</div>

<% end %>

